import React from 'react';
import { mount } from 'enzyme';
import { Login } from '../../pages/Login';

describe('<Login></Login>', () => {
	//const wrapper = mount(<Login />);

	test('render title', () => {
		/* const text = wrapper.find('h2').text();
		expect(text).toEqual('Chosse Account Type'); */
	});
});
